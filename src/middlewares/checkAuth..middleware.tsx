// import { RouteComponentIProps, RedirectFunction } from 'react-router';

// const checkAuthMiddleware = (isAuthenticated: boolean, authenticationPath: string) => {
//   return (IProps: RouteComponentIProps, replace: RedirectFunction) => {
//     if (!isAuthenticated) {
//       replace(authenticationPath);
//     }
//   };
// };